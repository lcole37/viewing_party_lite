<!DOCTYPE html>
<html>
  <head>
    <title>ViewingPartyLite</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <nav id="home_page_link">
    <%= link_to "Home", root_path %>
  </nav>
  <nav id="user_login" >
    <%= link_to "Log In", "/login" unless session[:user_id]%>
  </nav>
  <body>
    <% flash.each do |type, msg| %>
  <div>
    <%= msg %>
  </div>
  <% end %>
    <%= yield %>
  </body>
</html>
